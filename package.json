{
  "name": "employee-leave-system",
  "version": "1.0.0",
  "description": "Employee Leave Management System - Full Stack Application",
  "scripts": {
    "install:all": "npm ci && npm ci --prefix=client && npm ci --prefix=server",
    "build": "node build.js",
    "start": "node server/index.js",
    "dev:server": "npm run dev --prefix=server",
    "dev:client": "npm run dev --prefix=client"
  },
  "keywords": ["leave-management", "employee", "vue", "express", "mongodb"],
  "license": "MIT"
}
