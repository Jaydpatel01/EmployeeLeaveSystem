services:
  - type: web
    name: employee-leave-system
    runtime: node
    buildCommand: node build.js
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: NODE_ENV
        value: production
